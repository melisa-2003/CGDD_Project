using UnityEngine;

public class Readme
{
    // all main camera in each layer is temporary (use camrefollow2d script)
    // after opening, main menu and ending are done
    // they will be removed and replaced by a single main camera in boot (dont forget camerafollow2d)
    // so, u see all the layer i put scene init for each layer
    //the end
}
